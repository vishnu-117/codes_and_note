"""
re.match(): Matches at the start of the string.
re.search(): Searches for the pattern anywhere in the string.
re.findall(): Finds all occurrences and returns them as a list.
re.finditer(): Finds all occurrences and returns an iterator of match objects.
re.sub(): Replaces occurrences of the pattern with a replacement string.
re.split(): Splits the string by occurrences of the pattern.
re.compile(): Compiles a pattern into a regex object.
re.escape(): Escapes special characters in a string for use in a regex pattern.


"""